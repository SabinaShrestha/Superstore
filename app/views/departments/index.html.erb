<h1>Departments</h1>

<%= link_to 'Add Department', new_department_path %>

<% @departments.each do |department| %>
  <%= department.name %>
  <%= link_to 'View', department_path(department), {class: 'btn'}%>
<% end %>
